# 📊 統計機能 データ要件定義

## 📋 目次

1. [概要](#概要)
2. [機能範囲の決定事項](#機能範囲の決定事項)
3. [利用可能なデータソース](#利用可能なデータソース)
4. [統計ページ別データ要件](#統計ページ別データ要件)
5. [API実装計画](#api実装計画)

---

## 概要

BoxLogアプリの統計機能で表示すべきデータと、その実装要件を定義するドキュメントです。

### ステータス

- **作成日**: 2025-12-04
- **最終更新**: 2025-12-04
- **ステータス**: 🚧 要件定義中

---

## 機能範囲の決定事項

### ✅ 決定: Self Analysis系は統計から分離

**方針**: Self Analysis系（Goals, Principles, Value等）は **Settings > Personalization** に移動

| 移動先 | 項目 |
|--------|------|
| Settings > Personalization | Goals, Principles, Value, Life Vision, Identity, Purpose, AntiValues |

**理由**:
- 統計 = 数値データの可視化（何をどれだけやったか）
- Self Analysis = ユーザー情報の言語化（自分は何者か）
- コンテキストが異なるため分離
- ChatGPTのパーソナライズ設定と同様のUXを提供

### 統計ページの最終構成

```
Stats（統計）
├── Dashboard（概要）
├── Analytics
│   ├── Tasks（タスク統計）
│   ├── Time（時間分析）※sessions実装後
│   ├── Tags（タグ分析）
│   └── Trends（トレンド）
└── Reflect（振り返り）
    ├── Today（今日）
    ├── Week（週間）
    ├── Month（月間）
    └── All（全期間）
```

### 削除対象ページ（統計サイドバーから）

- `/stats/goals`
- `/stats/principles`
- `/stats/value`
- `/stats/life-vision`
- `/stats/identity`
- `/stats/purpose`
- `/stats/antivalues`
- `/stats/connpass`（外部API連携、別途検討）
- `/stats/act/try`（方針未定）
- `/stats/act/next`（方針未定）

---

## 利用可能なデータソース

### 実装済みテーブル

| テーブル | 説明 | 統計利用可能フィールド |
|---------|------|----------------------|
| `plans` | プラン（タスク） | status, created_at, due_date, start_time, end_time, recurrence_type, reminder_minutes |
| `plan_activities` | プラン変更履歴 | action_type, field_name, old_value, new_value, created_at |
| `plan_instances` | 繰り返しオカレンス | instance_date, is_exception, exception_type |
| `tags` | タグ（3階層） | name, level, is_active, color, created_at |
| `tag_groups` | タググループ | name, sort_order |
| `plan_tags` | プラン-タグ紐付け | plan_id, tag_id, created_at |
| `notifications` | 通知 | type, priority, is_read, created_at |
| `sessions` | 作業セッション | duration_minutes, actual_start, actual_end（将来機能） |

### ステータス体系

```
todo → doing → done
```

### 繰り返しタイプ

```
none | daily | weekly | monthly | yearly | weekdays
```

---

## 統計ページ別データ要件

### 1. Dashboard / Overview（ダッシュボード）

**目的**: 全体の活動状況を一目で把握

#### 必要なデータ

| 指標 | 計算方法 | データソース |
|------|---------|-------------|
| 完了プラン数 | `COUNT(plans WHERE status = 'done')` | plans |
| 進行中プラン数 | `COUNT(plans WHERE status = 'doing')` | plans |
| 未着手プラン数 | `COUNT(plans WHERE status = 'todo')` | plans |
| 完了率 | `done / total * 100` | plans |
| 作業時間 | `SUM(sessions.duration_minutes)` | sessions（将来） |
| 期間内作成数 | `COUNT(plans WHERE created_at BETWEEN ?)` | plans |
| 期間比較（前週比等） | 前期間との差分 | plans |

#### 表示形式

- サマリーカード（3-4枚）
- 週間/月間トレンドチャート（折れ線グラフ）
- ステータス分布（ドーナツチャート）

---

### 2. Analytics - Tasks（タスク統計）

**目的**: プラン/タスクの詳細分析

#### 必要なデータ

| 指標 | 計算方法 | 備考 |
|------|---------|------|
| ステータス別件数 | GROUP BY status | todo/doing/done |
| 曜日別作成数 | GROUP BY DAYOFWEEK(created_at) | 活動パターン |
| 時間帯別完了数 | GROUP BY HOUR(updated_at) WHERE status = 'done' | 生産性時間帯 |
| 期限遵守率 | `done before due_date / total with due_date` | 期限管理 |
| 期限超過件数 | `COUNT WHERE due_date < NOW() AND status != 'done'` | 要注意項目 |
| 繰り返しプラン比率 | `COUNT WHERE recurrence_type != 'none' / total` | 習慣化度 |

#### 表示形式

- ステータス別棒グラフ
- 曜日別ヒートマップ
- 時間帯別棒グラフ

---

### 3. Analytics - Time（時間分析）

**目的**: 時間の使い方を可視化

#### 必要なデータ（sessions実装後）

| 指標 | 計算方法 | 備考 |
|------|---------|------|
| 総作業時間 | `SUM(duration_minutes)` | 期間内 |
| 日別作業時間 | GROUP BY DATE(actual_start) | トレンド |
| 平均セッション時間 | `AVG(duration_minutes)` | 集中力指標 |
| 最長セッション | `MAX(duration_minutes)` | フロー状態 |
| タグ別作業時間 | JOIN plan_tags | カテゴリ分析 |

#### 現状

⚠️ `sessions`テーブルは将来機能のため、現時点では実装不可

---

### 4. Analytics - Categories / Tag Analysis（タグ分析）

**目的**: タグの使用状況と分布を分析

#### 必要なデータ

| 指標 | 計算方法 | 備考 |
|------|---------|------|
| タグ別プラン数 | `COUNT(plan_tags) GROUP BY tag_id` | 使用頻度 |
| タグ別完了率 | `done / total per tag` | タグ効率 |
| 階層別タグ数 | `COUNT(tags) GROUP BY level` | タグ構造 |
| 未使用タグ | `tags NOT IN plan_tags` | 整理候補 |
| タググループ別分布 | JOIN tag_groups | グループ分析 |
| タグ作成推移 | GROUP BY DATE(tags.created_at) | タグ管理 |

#### 表示形式

- タグ使用頻度ランキング（横棒グラフ）
- タグ別完了率（バブルチャート）
- タグ階層ツリーマップ

---

### 5. Analytics - Trends（トレンド）

**目的**: 長期的な傾向を把握

#### 必要なデータ

| 指標 | 計算方法 | 期間 |
|------|---------|------|
| 月別作成数推移 | GROUP BY MONTH(created_at) | 6-12ヶ月 |
| 月別完了数推移 | GROUP BY MONTH(updated_at) WHERE status='done' | 6-12ヶ月 |
| 週次完了率推移 | 週ごとの完了率計算 | 12-24週 |
| 活動量推移 | COUNT(plan_activities) per period | 6-12ヶ月 |

#### 表示形式

- 複合折れ線グラフ（作成 vs 完了）
- 完了率推移エリアチャート

---

### 6. Reflect（振り返り）

#### 6.1 Today（今日の振り返り）

| 指標 | 計算方法 |
|------|---------|
| 今日完了したプラン | `WHERE status='done' AND DATE(updated_at)=TODAY` |
| 今日作成したプラン | `WHERE DATE(created_at)=TODAY` |
| 今日のステータス変更履歴 | `plan_activities WHERE DATE(created_at)=TODAY` |
| 今日の作業時間 | sessions（将来） |

#### 6.2 Week（週間振り返り）

| 指標 | 計算方法 |
|------|---------|
| 今週の完了数 | 週間集計 |
| 曜日別活動量 | 7日間の日別データ |
| 前週との比較 | 差分計算 |
| 週間ハイライト | 最も活動的な日、最多完了日 |

#### 6.3 Month（月間振り返り）

| 指標 | 計算方法 |
|------|---------|
| 月間完了数 | 月間集計 |
| 週別推移 | 4-5週間のデータ |
| 前月との比較 | 差分計算 |
| 月間ハイライト | 達成率、活動パターン |

#### 6.4 All（全期間振り返り）

| 指標 | 計算方法 |
|------|---------|
| 累計プラン数 | 全期間集計 |
| 累計完了数 | 全期間集計 |
| 月別推移 | 全月のデータ |
| 成長曲線 | 活動量の変化 |

---

## 未定義事項

### Actions（Try/Next）の扱い

| ページ | 現状 | 検討案 |
|--------|------|--------|
| `/stats/act/try` | テーブルなし | plansのサブセット？特定タグ？ |
| `/stats/act/next` | テーブルなし | plansのサブセット？特定タグ？ |

→ **要検討**: 統計に残すか、別機能にするか

### Time（時間分析）

- `sessions`テーブルが将来機能のため、現時点では実装不可
- sessions実装後に対応

---

## API実装計画

### Phase 1: 基本統計（優先度: 高）

```typescript
// 実装すべきtRPCエンドポイント

plans.getStats          // ✅ 既存（拡張必要）
plans.getStatsByPeriod  // 🆕 期間指定統計
plans.getTrends         // 🆕 トレンドデータ
tags.getStats           // 🆕 タグ統計
tags.getUsageRanking    // 🆕 使用頻度ランキング
```

### Phase 2: 詳細統計（優先度: 中）

```typescript
plans.getActivityStats   // 🆕 活動統計
plans.getDueDateStats    // 🆕 期限関連統計
plans.getRecurrenceStats // 🆕 繰り返し統計
```

### Phase 3: 振り返り（優先度: 中）

```typescript
reflect.getDaily    // 🆕 日次振り返りデータ
reflect.getWeekly   // 🆕 週次振り返りデータ
reflect.getMonthly  // 🆕 月次振り返りデータ
reflect.getAll      // 🆕 全期間振り返りデータ
```

### Phase 4: 時間統計（優先度: 低）

```typescript
// sessions テーブル実装後
sessions.getStats      // 🆕 セッション統計
sessions.getTimeByTag  // 🆕 タグ別作業時間
```

---

## 次のステップ

- [x] Self Analysis系ページの方針決定 → **Settings > Personalization に移動**
- [ ] Actions（Try/Next）の方針決定
- [ ] 統計サイドバーからSelf Analysis系を削除
- [ ] API実装の優先順位確定
- [ ] 各APIの詳細仕様定義
- [ ] UIコンポーネントとの紐付け

---

## 関連Issue/作業

| 作業 | ステータス | 備考 |
|------|----------|------|
| 統計サイドバー整理 | 🔜 TODO | Self Analysis系の削除 |
| Settings > Personalization設計 | 🔜 TODO | データモデル含む |

---

**最終更新**: 2025-12-04
**ステータス**: 🚧 要件定義中
