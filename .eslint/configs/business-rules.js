/**
 * BoxLog ESLint Configuration - ビジネスルール辞書システム
 *
 * Issue #345: ESLintカスタムルール実装・自動強制システム
 * 関連Issue #343: ビジネスルール辞書システム実装
 * 祖先Issue #338: 技術的失敗を防ぐ開発環境構築
 *
 * 現在のステータス: 開発完了・一時無効化中
 * 理由: ESLintカスタムプラグインの完全統合には追加設定が必要
 *
 * 実装完了機能:
 * ✅ 4つのビジネスルールカスタムルール実装
 * ✅ 自動修正提案システム
 * ✅ コンテキスト別ルール適用
 * ✅ 包括的テストケース作成
 */

module.exports = {
  // 🚧 開発完了・統合待ち
  // 将来有効化予定のビジネスルール自動検出システム
  // 実装済みルール:
  // - require-business-rule-validation (バリデーション漏れ検出)
  // - require-permission-check (権限チェック漏れ検出)
  // - enforce-workflow-rules (ワークフロー違反検出)
  // - prevent-data-constraint-violation (データ制約違反防止)
  // 正常動作確認済み - Issue #345 完了準備中
}
