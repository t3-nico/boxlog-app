# ========================
# 🚀 BoxLog Environment Configuration Template
# ========================
# 新メンバーセットアップ用テンプレートファイル
#
# 🎯 クイックスタート:
# 1. cp .env.example .env.local
# 2. 下記の値を適切に設定
# 3. npm install && npm run dev
#
# 📚 詳細ドキュメント: docs/setup/ENVIRONMENT_SETUP.md

# ========================
# 🔐 環境別の設定方法
# ========================
#
# 【ローカル開発環境】
# → .env.local に直接記載
#
# 【CI/CD (GitHub Actions)】
# → GitHub Secrets を使用
# → workflow内で ${{ secrets.* }} で参照
# → 設定場所: GitHub Repository → Settings → Secrets and variables → Actions
#
# 【本番環境 (Vercel)】
# → Vercel Dashboard で環境変数設定
# → 自動デプロイ時に注入される
# → 設定場所: Vercel Project → Settings → Environment Variables
#

# ========================
# 🔐 Supabase Configuration
# ========================
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
SUPABASE_JWT_SECRET=your-jwt-secret-here

# ========================
# 🗄️ PostgreSQL Configuration
# ========================
POSTGRES_URL=postgresql://username:password@localhost:5432/boxlog_dev
POSTGRES_USER=your_username
POSTGRES_HOST=localhost
POSTGRES_PASSWORD=your_password
POSTGRES_DATABASE=boxlog_dev
POSTGRES_PRISMA_URL=postgresql://username:password@localhost:5432/boxlog_dev?pgbouncer=true&connect_timeout=15
POSTGRES_URL_NON_POOLING=postgresql://username:password@localhost:5432/boxlog_dev

# ========================
# 🐛 Sentry Configuration
# ========================
NEXT_PUBLIC_SENTRY_DSN=https://your-dsn@sentry.io/project-id
SENTRY_ORG=your-sentry-org
SENTRY_PROJECT=your-sentry-project
SENTRY_AUTH_TOKEN=your-auth-token

# ========================
# 🌐 Application Settings
# ========================
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# ========================
# 🎛️ Feature Flags
# ========================
# 開発中の機能の有効/無効を制御
FEATURE_DASHBOARD=true
FEATURE_ANALYTICS=false
FEATURE_DEBUG_MODE=true
SKIP_AUTH_IN_DEV=false

# ========================
# 🚧 Error Pages & Maintenance
# ========================
# メンテナンスモード（全アクセスをメンテナンスページにリダイレクト）
NEXT_PUBLIC_MAINTENANCE_MODE=false

# ========================
# 🔧 Development Settings
# ========================
# 開発効率化のための設定
NEXT_TELEMETRY_DISABLED=1
DISABLE_ERD=false

# ========================
# 📦 Version Information
# ========================
VERSION=0.1.0
BUILD_ID=local-dev

# ========================
# 🚨 セキュリティ注意事項
# ========================
# ❌ 絶対にやらないこと:
# - 実際の秘密情報をこのファイルにコミットしない
# - .env.local ファイルをGitに含めない
# - プロダクション環境の値をここに記載しない
#
# ✅ 推奨事項:
# - .env.local に環境変数を設定
# - 本番環境では Vercel Environment Variables を使用
# - 定期的に秘密情報をローテーションする
