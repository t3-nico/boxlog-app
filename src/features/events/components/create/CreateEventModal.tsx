'use client'

/**
 * CreateEventModal - Inspector統合により廃止
 * 
 * このコンポーネントはInspector統合により廃止されました。
 * 新規作成・編集は右側のInspectorパネルで行われます。
 * 後方互換性のため残していますが、何も表示しません。
 */
export const CreateEventModal = () => {
  return null
}

// Inspector用のエクスポート（後方互換性のため）
export const CreateEventInspector = CreateEventModal