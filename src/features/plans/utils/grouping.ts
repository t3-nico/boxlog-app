/**
 * Re-export from shared lib for backward compatibility
 * 実体は @/lib/plan-grouping に移動済み
 */
export { groupItems } from '@/lib/plan-grouping';
export type { GroupByField, GroupedData } from '@/lib/plan-grouping';
