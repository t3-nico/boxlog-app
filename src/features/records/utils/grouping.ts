/**
 * Re-export from shared lib for backward compatibility
 * 実体は @/lib/record-grouping に移動済み
 */
export { groupRecordItems } from '@/lib/record-grouping';
export type { RecordGroupByField } from '@/lib/record-grouping';
